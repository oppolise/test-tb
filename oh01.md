# ขั้นตอนการ Parsing ไฟล์ Trace จนได้ Operator Tree (`tid2tree`)

เอกสารนี้อธิบายขั้นตอนการทำงานของ PyTorch Profiler Plugin ตั้งแต่การรับไฟล์ trace เข้ามา จนกระทั่งประมวลผลเสร็จสิ้นและได้ผลลัพธ์เป็น `tid2tree` (Operator Tree) พร้อมทั้งชี้ให้เห็นว่าข้อมูลถูกส่งต่อไปอย่างไรและใครนำไปใช้ต่อ.

## ภาพรวมการไหลของข้อมูล (Data Flow)

```
[Trace File] -> RunLoader -> RunProfileData -> EventParser -> OpTreeBuilder
                                     ^             |
                                     |             v
                                     |         [tid2tree]
                                     |             |
                                     +-------------+
                                     |
                                     v
                  [Other Parsers: ModuleAggregator, MemoryParser, etc.]
                                     |
                                     v
                                RunGenerator -> [UI Data]
```

---

## ขั้นตอนที่ 1: การเริ่มต้นโหลดข้อมูล (`RunLoader`)

กระบวนการทั้งหมดเริ่มต้นที่คลาส `RunLoader` ซึ่งทำหน้าที่เป็นตัวจัดการการโหลดข้อมูลของ profiler run หนึ่งๆ.

*   **ไฟล์**: `tb_plugin/torch_tb_profiler/profiler/loader.py`
*   **โค้ดที่เกี่ยวข้อง**:
    ```python
    # loader.py -> class RunLoader
    def load(self):
        # ... (สแกนหาไฟล์ trace ใน run_dir) ...
        for worker, span, path in workers:
            # สร้าง child process เพื่อรัน _process_data สำหรับแต่ละไฟล์
            p = Process(target=self._process_data, args=(worker, span_index, path))
            p.start()
        # ... (รอและรวบรวมผลลัพธ์จาก Queue) ...

    def _process_data(self, worker, span, path):
        # ...
        # เรียก RunProfileData.parse ซึ่งเป็นจุดเริ่มต้นของการ parsing จริงๆ
        data = RunProfileData.parse(worker, span, local_file, self.caches.cache_dir)
        # ...
        self.queue.put((profile, dist_data))
    ```
*   **คำอธิบาย**: `RunLoader.load()` จะค้นหาไฟล์ trace ทั้งหมด และสำหรับแต่ละไฟล์, มันจะสร้าง process ใหม่เพื่อเรียกใช้ `_process_data`. ภายใน `_process_data`, จะมีการเรียก `RunProfileData.parse()` ซึ่งเป็นการส่งต่อหน้าที่การประมวลผลไฟล์นั้นๆ ไปยังคลาส `RunProfileData`.

---

## ขั้นตอนที่ 2: การเตรียมข้อมูลและสร้าง Event Objects

`RunProfileData` รับผิดชอบในการอ่านไฟล์ JSON และแปลงข้อมูลดิบให้เป็นโครงสร้างข้อมูลที่ใช้งานได้ง่ายขึ้น.

*   **ไฟล์**: `tb_plugin/torch_tb_profiler/profiler/data.py` และ `trace.py`
*   **โค้ดที่เกี่ยวข้อง**:
    ```python
    # data.py -> class RunProfileData
    @staticmethod
    def parse(worker, span, path, cache_dir):
        # เรียก _preprocess_file เพื่ออ่านและทำความสะอาด JSON
        trace_path, trace_json = RunProfileData._preprocess_file(path, cache_dir)
        # เรียก from_json เพื่อสร้าง instance และเริ่ม process
        profile = RunProfileData.from_json(worker, span, trace_json)
        return profile

    def __init__(self, worker: str, span: str, trace_json: Dict):
        # ...
        trace_body = trace_json['traceEvents']
        # วนลูปเพื่อสร้าง event objects
        for data in trace_body:
            if data.get('cat') == 'fwdbwd':
                fwd_bwd_events.append(data)
            else:
                # เรียก factory จาก trace.py
                event = trace.create_event(data, self.is_pytorch_lightning)
                if event is not None:
                    self.events.append(event)

        self.events.sort(key=lambda e: e.ts)
        # สร้าง map ความสัมพันธ์ forward-backward
        self.forward_backward_events = trace.create_association_events(fwd_bwd_events)
    ```
*   **คำอธิบาย**:
    1.  `RunProfileData.parse()` รับ path ของไฟล์เข้ามา, เรียกใช้ `_preprocess_file` เพื่ออ่านไฟล์, decompress, และจัดการกับ format ที่ผิดพลาดของ JSON จนได้เป็น Python dictionary (`trace_json`).
    2.  `__init__` ของ `RunProfileData` จะถูกเรียก. มันจะวนลูป `trace_json['traceEvents']` ทั้งหมด.
    3.  สำหรับแต่ละ event dictionary, มันจะเรียก `trace.create_event()` เพื่อแปลงให้เป็น typed object (เช่น `KernelEvent`, `OperatorEvent`).
    4.  event object ทั้งหมดจะถูกเก็บใน `self.events` และถูกเรียงตามเวลา (`ts`).
    5.  event พิเศษ `fwdbwd` จะถูกใช้สร้าง `self.forward_backward_events` map.
    6.  **ณ จุดนี้, ข้อมูลดิบได้ถูกแปลงเป็น input ที่พร้อมสำหรับ `EventParser` แล้ว**.

---

## ขั้นตอนที่ 3: การเรียกใช้ `EventParser` (Engine หลัก)

เมื่อข้อมูลพร้อม, `RunProfileData` จะเรียกใช้ `EventParser` เพื่อเริ่มการวิเคราะห์เชิงโครงสร้าง.

*   **ไฟล์**: `tb_plugin/torch_tb_profiler/profiler/data.py`
*   **โค้ดที่เกี่ยวข้อง**:
    ```python
    # data.py -> class RunProfileData
    def process(self):
        # ...
        parser = EventParser()
        # ส่ง list ของ event objects และ fwd/bwd map เข้าไปใน parser
        self.tid2tree, self.pl_tid2tree = parser.parse(self.events, self.forward_backward_events)
        # ...
    ```
*   **คำอธิบาย**: `RunProfileData.process()` คือจุดที่ `EventParser.parse()` ถูกเรียก. `EventParser` จะรับ `events` และ `forward_backward_events` เข้าไปและเริ่มกระบวนการสร้าง tree.

---

## ขั้นตอนที่ 4: การสร้าง Operator Tree (`OpTreeBuilder`)

`EventParser` จะใช้ `OpTreeBuilder` เพื่อสร้างโครงสร้าง tree จาก flat list ของ events.

*   **ไฟล์**: `tb_plugin/torch_tb_profiler/profiler/event_parser.py` และ `op_tree.py`
*   **โค้ดที่เกี่ยวข้อง**:
    ```python
    # event_parser.py -> class EventParser
    def parse(self, events, fwd_bwd_map):
        # 1. แปลง Event objects เป็น Node objects (เก็บใน tid2list)
        tid2list, tid2zero_rt_list, staled_device_nodes, pl_tid2list = self.parse_nodes(events)

        # 2. สร้างและเรียกใช้ OpTreeBuilder
        builder = OpTreeBuilder()
        tid2tree = builder.build_tree(tid2list, tid2zero_rt_list, staled_device_nodes, fwd_bwd_map=fwd_bwd_map)
        # ...
        return tid2tree, pl_tid2tree

    # op_tree.py -> class OpTreeBuilder
    def _build_tree_internal(self, host_node_list, ...):
        # ... (Stack-based algorithm เพื่อสร้างความสัมพันธ์ parent-child) ...
    ```
*   **คำอธิบาย**:
    1.  **`EventParser.parse_nodes()`**: ถูกเรียกก่อนเพื่อแปลง `Event` objects ให้เป็น `Node` objects (เช่น `OperatorNode`) และจัดกลุ่มตาม thread ID (`tid2list`).
    2.  **`OpTreeBuilder.build_tree()`**: รับ `tid2list` เข้ามา.
    3.  **`_build_tree_internal()`**: คือหัวใจหลักที่ใช้ Stack-based algorithm ในการวนลูป `tid2list` ที่เรียงตามเวลาแล้ว. มันจะสร้างความสัมพันธ์ parent-child โดยดูจากเวลาเริ่มต้นและสิ้นสุดของแต่ละ node.
    4.  หลังจากสร้าง tree พื้นฐานเสร็จ, `OpTreeBuilder` จะใช้ `fwd_bwd_map` เพื่อสังเคราะห์ `BackwardNode` และแทรกกลับเข้าไปใน tree.
    5.  **`OpTreeBuilder` คืนค่า `tid2tree` กลับมาให้ `EventParser`**.

---

## ขั้นตอนที่ 5: การ Return และการนำ `tid2tree` ไปใช้ต่อ

`tid2tree` ที่สร้างเสร็จแล้วจะถูกส่งกลับมาและกลายเป็นข้อมูลสำคัญสำหรับ parser อื่นๆ.

*   **ไฟล์**: `tb_plugin/torch_tb_profiler/profiler/data.py`
*   **โค้ดที่เกี่ยวข้อง**:
    ```python
    # data.py -> class RunProfileData -> def process(self)

    # 1. รับ tid2tree กลับมาและเก็บไว้
    self.tid2tree, self.pl_tid2tree = parser.parse(self.events, self.forward_backward_events)

    # 2. ส่ง tid2tree ไปให้ parser อื่นๆ ใช้งานต่อ

    # ModuleAggregator ใช้ tid2tree เพื่อรวมข้อมูล operator
    module_aggregator = ModuleAggregator()
    module_aggregator.aggregate(self.tid2tree)

    # TensorCoresParser ใช้ tid2tree เพื่อวิเคราะห์ op ที่ TC-eligible
    tensorcores_parser = TensorCoresParser.parse_events(self.tid2tree, ...)

    # MemoryParser ใช้ tid2tree เพื่อ map memory events เข้ากับ operators
    memory_parser = MemoryParser(memory_events)
    self.memory_snapshot = memory_parser.find_memory_nodes(self.tid2tree)
    ```
*   **คำอธิบาย**:
    1.  `EventParser.parse()` คืนค่า `tid2tree` กลับมายัง `RunProfileData.process()`.
    2.  `RunProfileData` เก็บผลลัพธ์นี้ไว้ใน `self.tid2tree`.
    3.  `self.tid2tree` ถูกใช้เป็น **Input** สำหรับ parser ตัวอื่นๆ ที่ถูกเรียกต่อมาใน `process()`:
        *   **`ModuleAggregator`**: ใช้ tree ในการรวมสถิติของ operator สำหรับ Operator View.
        *   **`TensorCoresParser`**: ใช้ tree ในการวิเคราะห์การใช้ Tensor Cores.
        *   **`MemoryParser`**: ใช้ tree ในการเชื่อมโยง memory events กับ operator.
    4.  ท้ายที่สุด, `RunProfileData` object ทั้งหมด (ที่มี `tid2tree` และผลการวิเคราะห์อื่นๆ อยู่ข้างใน) จะถูกส่งต่อไปยัง `RunGenerator` เพื่อสร้างข้อมูลสำหรับแสดงผลใน UI.
